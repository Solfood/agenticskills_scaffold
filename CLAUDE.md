# CLAUDE.md

This repo is `agenticskills_scaffold`: a Claude Code skills plugin.

Skills are the interface — their descriptions are auto-loaded; see `README.md` for the catalog.
Continuity is opt-in: `/resume` to re-anchor on prior work, `/handoff` to close a session.
Markers: `SCAF-<TRACK>-<NNNN>` — see `/marker`.
